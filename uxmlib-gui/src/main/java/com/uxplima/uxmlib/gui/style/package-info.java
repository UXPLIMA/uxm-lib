/**
 * The parts a menu is drawn from: a centred window title, the tile shape a tooltip is built in, a lore
 * builder, and the sounds a menu plays. The structure is here and the values it draws with — the glyphs, the
 * colours, the sound names — come from {@link com.uxplima.uxmlib.text.style.Theme} and from a config file, so
 * a server changes its look without a plugin release.
 */
@NullMarked
package com.uxplima.uxmlib.gui.style;

import org.jspecify.annotations.NullMarked;
