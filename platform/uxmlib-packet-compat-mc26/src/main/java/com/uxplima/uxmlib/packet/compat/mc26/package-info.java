/**
 * The 26.x implementation of the compat seam. Not API: the classes here are loaded only when the library
 * finds itself on a 26.x server, and are picked by {@code ServerCompats}, never named by a consumer.
 */
@NullMarked
package com.uxplima.uxmlib.packet.compat.mc26;

import org.jspecify.annotations.NullMarked;
