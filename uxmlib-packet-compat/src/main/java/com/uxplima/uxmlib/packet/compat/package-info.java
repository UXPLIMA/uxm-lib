/**
 * The seam for server internals that differ between the Minecraft lines uxmLib supports. One implementation
 * of {@link com.uxplima.uxmlib.packet.compat.ServerCompat} ships per line, each in its own artifact compiled
 * against that line's server.
 */
@NullMarked
package com.uxplima.uxmlib.packet.compat;

import org.jspecify.annotations.NullMarked;
