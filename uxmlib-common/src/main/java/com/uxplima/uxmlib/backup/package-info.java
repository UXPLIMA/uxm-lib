/**
 * The seam a plugin uses to be told to save before something copies its files.
 *
 * <p>One class. It is here rather than in a backup plugin so that a plugin can be backup-safe without
 * depending on any particular backup plugin, ours included.
 */
@NullMarked
package com.uxplima.uxmlib.backup;

import org.jspecify.annotations.NullMarked;
